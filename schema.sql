CREATE TABLE Dimensiones (
    nombre VARCHAR(50) PRIMARY KEY,
    descripcion VARCHAR(100)
    dimensiones VARCHAR(50),
    peso FLOAT
)

CREATE TABLE Tipo (
    nombre VARCHAR(50) PRIMARY KEY,
    descripcion VARCHAR(100),
    precio_base FLOAT,
)

